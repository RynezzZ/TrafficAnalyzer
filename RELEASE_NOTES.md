# TrafficAnalyzer 发布说明

本文档记录 TrafficAnalyzer 自公开发布以来的主要版本变化。

## v1.1.6（当前版本）

本版本重点改进多 PCAP 项目的导入、来源识别与证据导出流程，同时优化大型项目打开时的交互反馈和 CyberChef 编辑器配置。

### 新增功能

#### HTTP 证据导出

- 支持在 HTTP 流量列表中选中一条或多条请求后，按需导出为 CSV、Excel 或 PCAP，无需打包为 ZIP。
- CSV 和 Excel 仅包含已选 HTTP 事务及其关联的请求、响应内容。
- Excel 使用单个 `Transactions` 工作表，请求与响应位于同一行，并提供以下独立字段：
  - 请求头
  - 请求体
  - 响应头
  - 响应体
- 文本正文直接写入表格；二进制正文使用 Base64 表示，避免内容损坏。
- CSV 导出对可能被表格软件识别为公式的内容进行安全处理。

#### 原始 PCAP 证据提取

- 当原始 PCAP 文件仍然存在且文件 Hash 与导入记录一致时，可使用 tshark 从原文件提取证据包。
- 支持一次选择来自多个 PCAP 文件的 HTTP 事务，并将各来源的提取结果组合到同一个 PCAPNG 文件中。
- 为保证 TCP 重组和 HTTP 解析的完整性，导出时保留所选事务所属的完整 `tcp.stream`。
- 同一 TCP 长连接中如果还包含其他 HTTP 事务，这些事务也可能随完整 TCP Stream 一并导出。这是为避免证据包因缺少上下文而无法重组所采用的保守策略。
- 当原始文件不存在、无法读取或 Hash 不一致时，界面会明确提示；用户确认后可根据数据库中保存的 HTTP 内容生成合成 PCAP。
- 合成 PCAP 会被明确标记为非原始抓包，适合后续查看和交换，不应替代原始文件用于证明原始网络层事实。

#### 多 PCAP 导入与来源识别

- PCAP 导入支持一次选择多个文件。
- 多文件按照选择顺序逐个导入，界面显示当前进度，并支持取消后续队列。
- HTTP 列表按 PCAP 来源分配稳定的区分色，并提供来源提示，便于直观看出每条事务来自哪个文件。

#### 跨页选择

- HTTP 流量列表支持跨分页保留勾选状态。
- 导出和发送到工作台时，会使用所有分页中累计选中的事务。
- 分页区域显示已产生选择的页码及各页数量，可点击快速返回对应页面。
- 支持一键清空全部选择。
- 切换项目、来源、筛选条件或重新导入数据时会清理旧选择，避免误操作其他数据集。

### 体验优化

#### 大型项目打开流程

- 打开较大的数据库项目后立即关闭项目管理窗口，并显示醒目的全页面加载状态。
- 加载期间禁用重复打开操作，避免用户因界面缺少反馈而多次点击。
- 优化项目数据库连接复用，减少打开过程中不必要的重复连接和读取。

#### CyberChef 编辑器

- CyberChef 编辑器目录支持自动识别目录中的 `CyberChef*.html` 入口文件。
- 配置变更后可正确刷新或清空编辑器地址，避免继续显示已经失效的旧页面。
- 编辑器不可用时显示明确的配置引导。
- 配置页面进一步区分内嵌 CyberChef 编辑器与独立的 CyberChef REST API 执行服务，减少配置歧义。

### 稳定性与测试

- 增加多来源 PCAP 提取、TCP Stream 完整性和 HTTP 可识别性测试。
- 增加 CSV、单工作表 Excel、二进制正文及公式安全处理测试。
- 增加合成 PCAP 和 CyberChef 入口文件识别测试。
- WebShell 流程测试改用自包含的加密样本，降低对仓库外部示例文件的依赖。

### 升级说明

- v1.1.6 可继续打开 v1.1.5 创建的项目数据库。
- 使用原始 PCAP 证据提取功能时，需要原文件仍位于记录路径，并保证文件内容未发生变化。
- PCAP 提取依赖程序配置的 tshark；原文件校验或提取失败时仍可选择生成合成 PCAP。

---

## v1.1.5（初始发布版本）

v1.1.5 是 TrafficAnalyzer 的首个公开发布版本，建立了面向 PCAP 与 HTTP 流量分析的完整桌面工作流。

### 初始能力

- 导入 PCAP、Excel、CSV 和常见 Apache/Nginx Access Log。
- 使用独立 SQLite 项目保存 HTTP 流量、分析结果、规则命中、标签和工作状态。
- 配对并查看 HTTP 请求与响应，支持搜索、过滤、排序、分页、详情查看和正文预览。
- 提供网络 Flow、通信拓扑、远程访问、扫描、异常外联和心跳行为分析。
- 提供内置规则、OWASP CRS、自定义规则、威胁事件聚合以及 IP/URI 行为评分。
- 集成 CyberChef 编辑器、解密工作台、可复用处理流程和自定义 Go 脚本。
- 支持 WebShell 解密与 Fuzz、Java 反序列化分析、Class 反编译和代码反混淆。
- 支持从已捕获流量构造 HTTP 请求并在授权环境中进行重放验证。
- 提供项目管理、分析配置、离线桌面运行及 Wails 原生应用体验。

> v1.1.5 作为初始发布基线；后续版本的变化均以该版本为起点记录。
